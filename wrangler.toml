name = "cbot"
main = "telegram-multipurpose-worker.js"
compatibility_date = "2026-07-20"

# Secrets are intentionally not stored here.
# Add BOT_TOKEN, ADMIN_CHAT_ID, and optional CHANNEL_ID in Cloudflare dashboard.
#
# KV binding must be connected in the dashboard with this exact variable name:
# BOT_KV
